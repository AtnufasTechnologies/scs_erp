<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('hr_faculty_promotions', function (Blueprint $table) {
            $table->id();
            $table->unsignedBigInteger('faculty_id');
            $table->unsignedBigInteger('from_designation_id')->nullable();
            $table->unsignedBigInteger('to_designation_id');
            $table->unsignedBigInteger('from_grade_level_id')->nullable();
            $table->unsignedBigInteger('to_grade_level_id')->nullable();
            $table->unsignedBigInteger('from_pay_matrix_id')->nullable();
            $table->unsignedBigInteger('to_pay_matrix_id')->nullable();
            $table->date('effective_date');
            $table->enum('promotion_type', ['regular', 'adhoc', 'honorary', 'lateral'])->default('regular');
            $table->text('reason')->nullable();
            $table->text('remarks')->nullable();
            $table->string('order_number')->nullable();
            $table->date('order_date')->nullable();
            $table->string('attachment')->nullable();
            $table->enum('status', ['pending', 'approved', 'rejected', 'implemented'])->default('pending');
            $table->unsignedBigInteger('approved_by')->nullable();
            $table->timestamp('approved_at')->nullable();
            $table->unsignedBigInteger('created_by')->nullable();
            $table->timestamps();
            $table->softDeletes();

            // Foreign keys - add indexes but no constraints for now
            $table->index('faculty_id');
            $table->index('from_designation_id');
            $table->index('to_designation_id');
            $table->index('from_grade_level_id');
            $table->index('to_grade_level_id');
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('hr_faculty_promotions');
    }
};
